# Capability

AgenticOS kernel module for robot capability contracts.

This is the OS-facing abstraction that maps stable Agent APIs and skill
manifests onto lower ROS2 or hardware resources such as services, actions,
topics, Nav2, MoveIt, perception backends, and vendor drivers.

